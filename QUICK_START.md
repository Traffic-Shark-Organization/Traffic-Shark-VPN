# 🚀 Быстрый старт - Автоматическая установка

Этот скрипт автоматически развернет production-ready OpenVPN сервер со всеми рекомендациями из основной инструкции.

## ⚡ Быстрая установка (5-10 минут)

### На сервере выполните:

```bash
# 1. Скачайте скрипт
wget https://raw.githubusercontent.com/YOUR_REPO/main/scripts/auto-deploy-vpn.sh

# или если у вас уже есть репозиторий:
git clone https://github.com/YOUR_REPO.git
cd Traffic-Shark-VPN-main

# 2. Сделайте скрипт исполняемым
chmod +x scripts/auto-deploy-vpn.sh

# 3. Запустите установку
sudo ./scripts/auto-deploy-vpn.sh
```

### Или одной командой:

```bash
wget -O - https://raw.githubusercontent.com/YOUR_REPO/main/scripts/auto-deploy-vpn.sh | sudo bash
```

## 📝 Что делает скрипт?

Скрипт автоматически:

1. ✅ **Проверяет систему** - совместимость с Debian/Ubuntu
2. ✅ **Собирает настройки** - интерактивно запрашивает параметры
3. ✅ **Обновляет систему** - apt update && upgrade
4. ✅ **Устанавливает пакеты** - OpenVPN, Easy-RSA, Fail2Ban и др.
5. ✅ **Настраивает IP forwarding** - для маршрутизации трафика
6. ✅ **Создает CA** - Certificate Authority с 4096-bit RSA
7. ✅ **Генерирует сертификаты** - сервер + клиенты
8. ✅ **Конфигурирует OpenVPN** - оптимально для 50+ клиентов
9. ✅ **Настраивает Firewall** - UFW или iptables на выбор
10. ✅ **Устанавливает Fail2Ban** - защита от брутфорса
11. ✅ **Оптимизирует систему** - сетевые параметры, лимиты
12. ✅ **Настраивает автообновления** - безопасность
13. ✅ **Создает первого клиента** - готовый .ovpn файл
14. ✅ **Создает скрипты управления** - для удобной работы
15. ✅ **Настраивает автобэкапы** - ежедневно в 3:00

## 🎯 Интерактивные настройки

Во время установки скрипт спросит:

- **IP/домен сервера** (автоопределение)
- **Сетевой интерфейс** (автоопределение)  
- **Тип Firewall**: UFW или iptables
- **DNS серверы**: Cloudflare, Google или Quad9
- **Данные организации** для сертификатов
- **Имя первого клиента** (опционально)

## 📦 Что будет установлено?

### Пакеты:
- `openvpn` - VPN сервер
- `easy-rsa` - управление сертификатами
- `fail2ban` - защита от атак
- `ufw` или `iptables-persistent` - firewall
- `unattended-upgrades` - автообновления
- Утилиты: `htop`, `net-tools`, `curl`, `wget`, `vim`

### Конфигурации:
- `/etc/openvpn/server.conf` - конфигурация сервера
- `/etc/openvpn/easy-rsa/` - PKI и сертификаты
- `~/client-configs/` - шаблоны и файлы клиентов

### Скрипты управления (`/root/scripts/`):
- `create-client.sh` - создание новых клиентов
- `revoke-client.sh` - отзыв сертификатов
- `monitor-vpn.sh` - мониторинг подключений
- `backup-vpn.sh` - резервное копирование

## 💡 После установки

### Скачать конфигурацию клиента:

```bash
# На вашем локальном компьютере:
scp root@IP_СЕРВЕРА:~/client-configs/files/client1.ovpn ./
```

### Создать нового клиента:

```bash
/root/scripts/create-client.sh client2
```

### Проверить статус:

```bash
systemctl status openvpn@server
/root/scripts/monitor-vpn.sh
```

### Посмотреть логи:

```bash
tail -f /var/log/openvpn/openvpn.log
journalctl -u openvpn@server -f
```

## 🔧 Управление

### Основные команды:

```bash
# Перезапуск сервера
systemctl restart openvpn@server

# Статус
systemctl status openvpn@server

# Подключенные клиенты
cat /var/log/openvpn/openvpn-status.log

# Создать клиента
/root/scripts/create-client.sh имя_клиента

# Отозвать клиента
/root/scripts/revoke-client.sh имя_клиента

# Мониторинг
/root/scripts/monitor-vpn.sh

# Резервное копирование
/root/scripts/backup-vpn.sh
```

## 🍎 Для macOS клиентов

Если OpenVPN Connect на Mac бесконечно подключается:

```bash
# Используйте Tunnelblick вместо OpenVPN Connect
brew install --cask tunnelblick

# Или исправьте конфигурацию:
/root/scripts/fix-client-for-mac.sh client1.ovpn client1-mac.ovpn
```

## 🔒 Безопасность

Скрипт настраивает максимальную безопасность:

- ✅ **TLS 1.3** - только современные протоколы
- ✅ **AES-256-GCM** - современное шифрование
- ✅ **4096-bit RSA** - сильные ключи
- ✅ **SHA512** - надежная аутентификация
- ✅ **Fail2Ban** - защита от брутфорса
- ✅ **Firewall** - только нужные порты открыты
- ✅ **Автообновления** - патчи безопасности
- ✅ **User nobody** - запуск без привилегий

## 📊 Производительность

Оптимизировано для 50+ одновременных клиентов:

- ✅ Увеличенные буферы (393216)
- ✅ TCP BBR congestion control
- ✅ Увеличенные системные лимиты (65536 файлов)
- ✅ Оптимизированные сетевые параметры
- ✅ Fast-IO режим OpenVPN

## 💾 Резервное копирование

Автоматические бэкапы настроены:

- **Частота**: Ежедневно в 3:00
- **Хранение**: 30 дней
- **Расположение**: `/root/vpn-backups/`
- **Что сохраняется**:
  - Все сертификаты и ключи
  - Конфигурации сервера и клиентов
  - Настройки firewall
  - Настройки Fail2Ban

### Восстановление:

```bash
tar -xzf /root/vpn-backups/openvpn-backup-ДАТА.tar.gz -C /
systemctl restart openvpn@server
```

## 🐛 Устранение неполадок

### Сервер не запускается:

```bash
# Проверить логи
journalctl -u openvpn@server -n 50

# Проверить конфигурацию
openvpn --config /etc/openvpn/server.conf --test-crypto

# Проверить сертификаты
ls -la /etc/openvpn/
```

### Клиент не подключается:

```bash
# Проверить порт
netstat -tulpn | grep 1194

# Проверить firewall
ufw status verbose
# или
iptables -L -n -v

# Проверить IP forwarding
sysctl net.ipv4.ip_forward
```

### Медленная скорость:

```bash
# Проверить нагрузку
htop

# Проверить сеть
iftop -i tun0

# Тест скорости
wget -O /dev/null http://speedtest.tele2.net/100MB.zip
```

## 📚 Полная документация

Подробная инструкция с объяснениями: [README.md](README.md)

## ⚠️ Требования

- **ОС**: Debian 11+, Ubuntu 20.04+ (рекомендуется Debian 13)
- **Права**: Root доступ
- **VPS минимум**:
  - 2 vCPU (4 vCPU для 50+ клиентов)
  - 2 GB RAM (4 GB рекомендуется)
  - 20 GB SSD
  - 100 Mbps сеть

## 🎓 Что дальше?

1. **Настройте SSH ключи** - отключите парольную аутентификацию
2. **Создайте клиентов** - для всех пользователей
3. **Настройте мониторинг** - добавьте алерты
4. **Проверьте бэкапы** - убедитесь что работают
5. **Изучите документацию** - README.md содержит много полезного

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `journalctl -u openvpn@server`
2. Запустите мониторинг: `/root/scripts/monitor-vpn.sh`
3. Изучите [полную документацию](README.md)
4. Проверьте раздел [Troubleshooting](README.md#troubleshooting)

---

**Автор**: Traffic Shark VPN Infrastructure Team  
**Версия**: 1.0.0  
**Лицензия**: MIT

