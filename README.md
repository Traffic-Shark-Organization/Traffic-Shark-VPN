# OpenVPN Server: Корпоративная инфраструктура для 50+ клиентов

## 📋 Оглавление

1. [Введение](#введение)
2. [Требования и спецификации](#требования-и-спецификации)
3. [Архитектура решения](#архитектура-решения)
4. [Установка и настройка](#установка-и-настройка)
5. [Управление сертификатами](#управление-сертификатами)
6. [Настройка безопасности](#настройка-безопасности)
7. [Мониторинг и логирование](#мониторинг-и-логирование)
8. [Масштабирование](#масштабирование)
9. [Резервное копирование](#резервное-копирование)
10. [Troubleshooting](#troubleshooting)

---

## 🎯 Введение

Данная инструкция описывает развертывание production-ready OpenVPN сервера для корпоративного использования с поддержкой 50+ одновременных подключений.

### 🚀 Два способа установки

**1. Автоматическая установка (рекомендуется для быстрого старта)**
- ⚡ Установка за 5-10 минут
- ✅ Все настройки оптимизированы автоматически
- 🛡️ Безопасная конфигурация по умолчанию

👉 **[Перейти к автоматической установке](INSTALLATION.md#автоматическая-установка)** | **[Быстрый старт](QUICK_START.md)**

**2. Ручная установка (этот документ)**
- 🔧 Полный контроль над каждым шагом
- 📚 Понимание работы всех компонентов
- 🎓 Образовательная ценность

👉 **Продолжайте читать этот документ для ручной установки**

### 📁 О конфигурационных файлах

В этом репозитории есть директория `configs/` с готовыми конфигурациями. Эти файлы находятся на вашем **локальном компьютере**, а не на сервере.

**Способы использования:**
1. **Копировать содержимое** прямо в терминал сервера (рекомендуется) - все команды в инструкции уже содержат готовый код
2. **Загрузить через SCP** с локального компьютера:
   ```bash
   scp configs/имя-файла root@IP_СЕРВЕРА:/путь/на/сервере
   ```

### Ключевые особенности решения:

- ✅ **Безопасность**: TLS 1.3, AES-256-GCM шифрование, 4096-bit RSA ключи
- ✅ **Масштабируемость**: оптимизация для 50+ одновременных подключений
- ✅ **Мониторинг**: интеграция с Prometheus, централизованное логирование
- ✅ **Автоматизация**: скрипты для управления пользователями и сертификатами
- ✅ **Отказоустойчивость**: автоматический перезапуск, health checks
- ✅ **Audit**: полное логирование подключений и действий

---

## 📊 Требования и спецификации

### Минимальные требования VPS:

- **CPU**: 2 vCPU (рекомендуется 4 vCPU для 50+ клиентов)
- **RAM**: 2 GB (рекомендуется 4 GB)
- **Диск**: 20 GB SSD
- **Сеть**: 100 Mbps (рекомендуется 1 Gbps)
- **OS**: Debian 13 (Trixie)

### Сетевая архитектура:

- **VPN subnet**: 10.8.0.0/24 (до 254 клиентов)
- **Протокол**: UDP 1194 (основной), TCP 443 (резервный)
- **DNS**: собственный или публичный (Cloudflare, Google)

---

## 🏗️ Архитектура решения

```
Internet
    ↓
[Firewall/iptables]
    ↓
[OpenVPN Server] ← [Easy-RSA CA]
    ↓                    ↓
[VPN Network]      [Certificates]
10.8.0.0/24             ↓
    ↓              [Client Configs]
[Клиенты 1-50+]
```

### Компоненты системы:

1. **OpenVPN Server** - основной VPN сервер
2. **Easy-RSA PKI** - управление сертификатами (Certificate Authority)
3. **iptables** - firewall и NAT
4. **systemd** - управление сервисами
5. **rsyslog** - централизованное логирование
6. **fail2ban** - защита от брутфорса

---

## 🚀 Установка и настройка

### Шаг 1: Подготовка системы

```bash
# Обновление системы
apt update && apt upgrade -y

# Установка необходимых пакетов
# ВАЖНО: ufw и iptables-persistent конфликтуют - выберите один вариант

# Вариант 1: С UFW (рекомендуется - проще в использовании)
apt install -y \
    openvpn \
    easy-rsa \
    iptables \
    fail2ban \
    htop \
    net-tools \
    curl \
    wget \
    vim \
    ufw

# Вариант 2: С iptables-persistent (для продвинутых пользователей)
# apt install -y \
#     openvpn \
#     easy-rsa \
#     iptables \
#     iptables-persistent \
#     fail2ban \
#     htop \
#     net-tools \
#     curl \
#     wget \
#     vim

# Проверка версий
openvpn --version
# OpenVPN 2.6.x ожидается на Debian 13
```

### Шаг 2: Настройка переадресации IP (IP Forwarding)

```bash
# Включение IP forwarding
echo "net.ipv4.ip_forward=1" >> /etc/sysctl.conf
echo "net.ipv6.conf.all.forwarding=1" >> /etc/sysctl.conf

# Применение настроек
sysctl -p

# Проверка
sysctl net.ipv4.ip_forward
# Должно быть: net.ipv4.ip_forward = 1
```

### Шаг 3: Настройка Certificate Authority (CA)

```bash
# Создание директории для Easy-RSA
make-cadir /etc/openvpn/easy-rsa
cd /etc/openvpn/easy-rsa
```

**Настройка vars файла:**

Вариант А - Создать на сервере (быстрый способ):

```bash
cat > vars << 'EOF'
# Easy-RSA 3 Parameter Settings
set_var EASYRSA_ALGO rsa
set_var EASYRSA_KEY_SIZE 4096
set_var EASYRSA_DIGEST "sha512"
set_var EASYRSA_CA_EXPIRE 3650
set_var EASYRSA_CERT_EXPIRE 730
set_var EASYRSA_CRL_DAYS 180

# Distinguished Name параметры (отредактируйте под вашу организацию)
set_var EASYRSA_REQ_COUNTRY "RU"
set_var EASYRSA_REQ_PROVINCE "Moscow"
set_var EASYRSA_REQ_CITY "Moscow"
set_var EASYRSA_REQ_ORG "Traffic Shark VPN"
set_var EASYRSA_REQ_EMAIL "admin@trafficshark.local"
set_var EASYRSA_REQ_OU "IT Security"

set_var EASYRSA_BATCH "yes"
set_var EASYRSA_SSL_CONF "$EASYRSA/openssl-easyrsa.cnf"
EOF
```

Вариант Б - Загрузить с локального компьютера:

```bash
# На вашем ЛОКАЛЬНОМ компьютере выполните:
scp configs/easy-rsa-vars root@YOUR_SERVER_IP:/etc/openvpn/easy-rsa/vars
```

Полный файл `configs/easy-rsa-vars` доступен в репозитории с подробными комментариями.

```bash
# Инициализация PKI
./easyrsa init-pki

# Создание CA (без пароля для автоматизации)
./easyrsa build-ca nopass

# Генерация ключей Diffie-Hellman (занимает время)
./easyrsa gen-dh

# Генерация TLS-auth ключа (защита от DoS)
openvpn --genkey secret /etc/openvpn/easy-rsa/pki/ta.key

# Генерация серверного сертификата
./easyrsa build-server-full server nopass

# Копирование файлов в директорию OpenVPN
cp pki/ca.crt /etc/openvpn/
cp pki/issued/server.crt /etc/openvpn/
cp pki/private/server.key /etc/openvpn/
cp pki/dh.pem /etc/openvpn/
cp pki/ta.key /etc/openvpn/

# Установка правильных прав доступа
chmod 600 /etc/openvpn/server.key
chmod 600 /etc/openvpn/ta.key
```

### Шаг 4: Конфигурация OpenVPN сервера

**Создание конфигурации сервера:**

```bash
# Создание директории для логов
mkdir -p /var/log/openvpn

# Создание конфигурации сервера
cat > /etc/openvpn/server.conf << 'EOF'
# OpenVPN Server Configuration для корпоративного VPN 50+ клиентов
port 1194
proto udp
dev tun

# SSL/TLS параметры
ca /etc/openvpn/ca.crt
cert /etc/openvpn/server.crt
key /etc/openvpn/server.key
dh /etc/openvpn/dh.pem
tls-auth /etc/openvpn/ta.key 0

# TLS настройки (максимальная безопасность)
tls-version-min 1.3
tls-cipher TLS-ECDHE-RSA-WITH-AES-256-GCM-SHA384:TLS-ECDHE-ECDSA-WITH-AES-256-GCM-SHA384
cipher AES-256-GCM
auth SHA512
tls-ciphersuites TLS_AES_256_GCM_SHA384

# Сетевые настройки
server 10.8.0.0 255.255.255.0
topology subnet
ifconfig-pool-persist /var/log/openvpn/ipp.txt

# Редирект всего трафика через VPN
push "redirect-gateway def1 bypass-dhcp"

# DNS серверы Cloudflare
push "dhcp-option DNS 1.1.1.1"
push "dhcp-option DNS 1.0.0.1"

# Разрешить клиентам видеть друг друга
client-to-client

# Производительность
max-clients 100
keepalive 10 120
sndbuf 393216
rcvbuf 393216
push "sndbuf 393216"
push "rcvbuf 393216"
fast-io

# Отключение compression (безопасность)
compress migrate
push "compress migrate"

# Безопасность
user nobody
group nogroup
persist-key
persist-tun
crl-verify /etc/openvpn/crl.pem
duplicate-cn
remote-cert-tls client
replay-window 64 15

# Логирование
verb 3
mute 20
status /var/log/openvpn/openvpn-status.log 1
log-append /var/log/openvpn/openvpn.log

# Безопасность renegotiation
reneg-sec 0
tls-timeout 120
EOF

# Проверка конфигурации (выполнится после создания сертификатов)
# openvpn --config /etc/openvpn/server.conf --test-crypto
```

> 💡 **Примечание**: Полная версия конфигурации с подробными комментариями доступна в `configs/server.conf` репозитория. Вы можете загрузить её с локального компьютера командой:
> ```bash
> scp configs/server.conf root@YOUR_SERVER_IP:/etc/openvpn/server.conf
> ```

Основные параметры конфигурации:

- **Протокол**: UDP (более быстрый для VPN)
- **Порт**: 1194 (стандартный)
- **Шифрование**: AES-256-GCM
- **Аутентификация**: SHA512
- **Compression**: отключено (безопасность > производительность)
- **Keepalive**: 10 120 (обнаружение мертвых соединений)

### Шаг 5: Настройка NAT и Firewall

**Вариант А: Настройка UFW (рекомендуется)**

```bash
# ВАЖНО: Сначала разрешите SSH, иначе потеряете доступ!
ufw allow OpenSSH
ufw allow 22/tcp  # если используете стандартный порт

# Разрешить OpenVPN порт
ufw allow 1194/udp

# Настройка IP forwarding для UFW
nano /etc/ufw/sysctl.conf
# Раскомментируйте строку:
# net.ipv4.ip_forward=1
# net.ipv6.conf.all.forwarding=1

# Настройка NAT в UFW
nano /etc/ufw/before.rules
```

В файле `/etc/ufw/before.rules` добавьте **ПЕРЕД** строкой `*filter`:

```bash
# NAT table rules for OpenVPN
*nat
:POSTROUTING ACCEPT [0:0]

# Определите ваш сетевой интерфейс: ip route | grep default
# Замените eth0 на ваш интерфейс (eth0, ens3, venet0, и т.д.)
-A POSTROUTING -s 10.8.0.0/24 -o eth0 -j MASQUERADE

COMMIT
```

Также в секции `*filter` добавьте правила для tun0:

```bash
# Allow traffic from OpenVPN clients
-A ufw-before-input -i tun0 -j ACCEPT
-A ufw-before-forward -i tun0 -j ACCEPT
-A ufw-before-forward -m state --state RELATED,ESTABLISHED -j ACCEPT
```

```bash
# Включить UFW
ufw enable

# Проверить статус
ufw status verbose

# Проверить правила
ufw status numbered
```

**Вариант Б: Настройка iptables-persistent (продвинутый)**

```bash
# Определение сетевого интерфейса
ip route | grep default
# Обычно это eth0, ens3, или venet0

# Экспорт переменной (замените на ваш интерфейс)
export INTERFACE="eth0"

# Настройка NAT через iptables
iptables -t nat -A POSTROUTING -s 10.8.0.0/24 -o $INTERFACE -j MASQUERADE

# Разрешение трафика через VPN
iptables -A INPUT -i tun0 -j ACCEPT
iptables -A FORWARD -i tun0 -j ACCEPT
iptables -A FORWARD -m state --state RELATED,ESTABLISHED -j ACCEPT

# Разрешение OpenVPN порта
iptables -A INPUT -p udp --dport 1194 -j ACCEPT

# Разрешение SSH (ВАЖНО!)
iptables -A INPUT -p tcp --dport 22 -j ACCEPT
iptables -A INPUT -m state --state RELATED,ESTABLISHED -j ACCEPT

# Сохранение правил
iptables-save > /etc/iptables/rules.v4
netfilter-persistent save
netfilter-persistent reload
```

Используйте скрипт `scripts/setup-firewall.sh` для автоматической настройки.

### Шаг 6: Запуск и включение автозапуска

```bash
# Запуск OpenVPN сервера
systemctl start openvpn@server

# Проверка статуса
systemctl status openvpn@server

# Включение автозапуска при загрузке
systemctl enable openvpn@server

# Проверка логов
journalctl -u openvpn@server -f

# Проверка интерфейса tun0
ip addr show tun0
```

---

## 🔐 Управление сертификатами

### Создание клиентского сертификата

Используйте скрипт `scripts/create-client.sh`:

```bash
# Сделать скрипт исполняемым
chmod +x scripts/create-client.sh

# Создание клиента
./scripts/create-client.sh client1

# Файл будет создан в ~/client-configs/files/client1.ovpn
```

### Ручное создание клиента

```bash
cd /etc/openvpn/easy-rsa

# Генерация сертификата клиента
./easyrsa build-client-full client1 nopass

# Создание директории для конфигураций
mkdir -p ~/client-configs/files

# Копирование базовой конфигурации
cp configs/client-base.conf ~/client-configs/base.conf

# Создание .ovpn файла (см. скрипт выше)
```

### Отзыв сертификата клиента

```bash
cd /etc/openvpn/easy-rsa

# Отзыв сертификата
./easyrsa revoke client1

# Генерация CRL (Certificate Revocation List)
./easyrsa gen-crl

# Копирование CRL
cp pki/crl.pem /etc/openvpn/

# Перезапуск OpenVPN
systemctl restart openvpn@server
```

Используйте скрипт `scripts/revoke-client.sh`.

### Список активных сертификатов

```bash
# Просмотр всех выданных сертификатов
cat /etc/openvpn/easy-rsa/pki/index.txt

# Просмотр отозванных сертификатов
cat /etc/openvpn/easy-rsa/pki/index.txt | grep "^R"

# Просмотр валидных сертификатов
cat /etc/openvpn/easy-rsa/pki/index.txt | grep "^V"
```

---

## 🔒 Настройка безопасности

### 1. Fail2Ban для защиты от брутфорса

```bash
# Установка Fail2Ban
apt install fail2ban -y

# Копирование конфигурации
cp configs/fail2ban-openvpn.conf /etc/fail2ban/jail.d/openvpn.conf

# Перезапуск Fail2Ban
systemctl restart fail2ban

# Проверка статуса
fail2ban-client status openvpn
```

### 2. Усиление безопасности OpenVPN

В конфигурации сервера уже включено:

- ✅ `tls-auth` - защита от DoS и сканирования портов
- ✅ `cipher AES-256-GCM` - современное шифрование
- ✅ `auth SHA512` - сильная аутентификация
- ✅ `tls-version-min 1.3` - только TLS 1.3
- ✅ `user nobody / group nogroup` - запуск от непривилегированного пользователя

### 3. Регулярное обновление системы

```bash
# Автоматические обновления безопасности
apt install unattended-upgrades -y
dpkg-reconfigure -plow unattended-upgrades

# Ручное обновление
apt update && apt upgrade -y
```

### 4. SSH hardening

```bash
# Редактирование SSH конфигурации
vim /etc/ssh/sshd_config

# Рекомендуемые настройки:
# PermitRootLogin no .
# PasswordAuthentication no .
# PubkeyAuthentication yes .
# Port 2222  # Смена порта (опционально)

# Перезапуск SSH
systemctl restart sshd
```

### 5. Аудит безопасности

```bash
# Установка инструментов аудита
apt install lynis aide -y

# Запуск аудита
lynis audit system

# Инициализация AIDE (детектор изменения файлов)
aideinit
cp /var/lib/aide/aide.db.new /var/lib/aide/aide.db
```

---

## 📊 Мониторинг и логирование

### 1. Мониторинг подключений

Используйте скрипт `scripts/monitor-connections.sh`:

```bash
chmod +x scripts/monitor-connections.sh
./scripts/monitor-connections.sh
```

### 2. Анализ логов

```bash
# Основные логи OpenVPN
tail -f /var/log/openvpn/openvpn.log

# Логи через systemd
journalctl -u openvpn@server -f

# Статистика подключений
grep "CONNECTED" /var/log/openvpn/openvpn.log | wc -l

# Последние подключения
grep "CONNECTED" /var/log/openvpn/openvpn.log | tail -20
```

### 3. Мониторинг производительности

```bash
# Загрузка CPU и памяти
htop

# Статистика сети
iftop -i tun0

# Использование диска
df -h

# Количество подключенных клиентов
cat /var/log/openvpn/openvpn-status.log | grep "CLIENT_LIST" | wc -l
```

### 4. Настройка алертов (опционально)

Используйте Prometheus + Alertmanager или простые скрипты с отправкой в Telegram/Email.

См. `scripts/send-alert.sh` для примера.

---

## 📈 Масштабирование

### Оптимизация для 50+ клиентов

1. **Увеличение лимитов системы**:

```bash
# Редактирование limits.conf
vim /etc/security/limits.conf

# Добавить:
* soft nofile 65536
* hard nofile 65536
```

2. **Оптимизация сетевых параметров**:

```bash
# Редактирование sysctl.conf
vim /etc/sysctl.conf

# Добавить:
net.core.rmem_max = 134217728
net.core.wmem_max = 134217728
net.ipv4.tcp_rmem = 4096 87380 67108864
net.ipv4.tcp_wmem = 4096 65536 67108864
net.core.netdev_max_backlog = 5000

# Применить
sysctl -p
```

3. **Конфигурация OpenVPN**:

В `server.conf` уже настроено:
- `max-clients 100` - максимум клиентов
- `sndbuf 393216` - размер буфера отправки
- `rcvbuf 393216` - размер буфера приема

### Масштабирование при росте клиентов (100+)

1. **Горизонтальное масштабирование**:
   - Развертывание нескольких VPN серверов
   - DNS round-robin или load balancer
   - Разделение клиентов по географии/отделам

2. **Вертикальное масштабирование**:
   - Увеличение ресурсов VPS
   - Переход на выделенный сервер

---

## 💾 Резервное копирование

### Критические данные для бэкапа:

1. CA и серверные ключи: `/etc/openvpn/easy-rsa/pki/`
2. Конфигурация сервера: `/etc/openvpn/server.conf`
3. Клиентские конфигурации: `~/client-configs/`
4. Firewall правила: `/etc/iptables/rules.v4`

### Автоматический бэкап

Используйте скрипт `scripts/backup.sh`:

```bash
# Сделать исполняемым
chmod +x scripts/backup.sh

# Запуск бэкапа
./scripts/backup.sh

# Настройка cron для ежедневного бэкапа
crontab -e

# Добавить:
0 3 * * * /root/scripts/backup.sh
```

### Восстановление из бэкапа

```bash
# Распаковка бэкапа
tar -xzf openvpn-backup-YYYY-MM-DD.tar.gz -C /

# Перезапуск сервисов
systemctl restart openvpn@server
systemctl restart iptables
```

---

## 🔧 Troubleshooting

### Проблема: Клиент не может подключиться

**Диагностика**:
```bash
# Проверка статуса сервера
systemctl status openvpn@server

# Проверка портов
netstat -tulpn | grep 1194

# Проверка firewall
iptables -L -n -v

# Проверка логов
tail -100 /var/log/openvpn/openvpn.log
```

**Решения**:
- Проверить, что порт 1194/UDP открыт на VPS (настройки провайдера)
- Проверить firewall правила
- Убедиться, что IP forwarding включен

### Проблема: macOS - Бесконечное подключение в OpenVPN Connect

Если на Mac OpenVPN Connect пытается подключиться бесконечно без ошибок, это обычно связано с несовместимостью конфигурации.

**Быстрое решение - использовать скрипт исправления**:

```bash
# На сервере запустите скрипт для исправления конфигурации
cd ~/client-configs/files
chmod +x /path/to/scripts/fix-client-for-mac.sh
/path/to/scripts/fix-client-for-mac.sh client1.ovpn client1-mac.ovpn

# Скопируйте исправленный файл на Mac и используйте его
```

**Ручное исправление в .ovpn файле**:

1. **Понизить TLS версию** - замените `tls-version-min 1.3` на `tls-version-min 1.2`
2. **Удалить user/group** - удалите строки `user nobody` и `group nogroup`
3. **Удалить explicit-exit-notify** - удалите строку `explicit-exit-notify 3`
4. **Упростить tls-cipher** - оставьте только один cipher suite или удалите строку
5. **Заменить compress migrate** - замените на `comp-lzo no` или удалите

**Альтернатива - использовать Tunnelblick**:

Tunnelblick более стабилен на Mac:
```bash
brew install --cask tunnelblick
```

Подробное решение смотрите в [TROUBLESHOOTING.md](TROUBLESHOOTING.md#проблема-11-macos-бесконечное-подключение-в-openvpn-connect)

### Проблема: Медленная скорость соединения

**Диагностика**:
```bash
# Тест скорости на сервере
wget -O /dev/null http://speedtest.tele2.net/100MB.zip

# Проверка нагрузки
htop

# Проверка сетевых интерфейсов
ip -s link
```

**Решения**:
- Переключиться на TCP 443 порт (если UDP блокируется)
- Изменить cipher на более быстрый (AES-128-GCM)
- Увеличить ресурсы VPS
- Оптимизировать MTU: `tun-mtu 1500`

### Проблема: Сертификат истек

```bash
# Проверка срока действия сертификата
openssl x509 -in /etc/openvpn/server.crt -noout -dates

# Перегенерация серверного сертификата
cd /etc/openvpn/easy-rsa
./easyrsa renew server nopass
```

### Проблема: Высокая нагрузка на CPU

**Решения**:
- Использовать аппаратное ускорение AES (если поддерживается)
- Уменьшить частоту keepalive
- Рассмотреть WireGuard как альтернативу (более производительный)

---

## 📚 Полезные команды

```bash
# Статус сервера
systemctl status openvpn@server

# Перезапуск сервера
systemctl restart openvpn@server

# Просмотр подключенных клиентов
cat /var/log/openvpn/openvpn-status.log

# Тест конфигурации
openvpn --config /etc/openvpn/server.conf --test-crypto

# Просмотр маршрутов
ip route show table main

# Статистика трафика
iftop -i tun0

# Проверка сертификатов
openssl x509 -in /etc/openvpn/ca.crt -noout -text
```

---

## 📖 Дополнительные ресурсы

- [Официальная документация OpenVPN](https://openvpn.net/community-resources/)
- [OpenVPN Security Best Practices](https://community.openvpn.net/openvpn/wiki/Hardening)
- [Easy-RSA Documentation](https://easy-rsa.readthedocs.io/)
- [Debian Security Handbook](https://www.debian.org/doc/manuals/securing-debian-manual/)

---

## ⚠️ Важные замечания

1. **Регулярно обновляйте систему** - критично для безопасности
2. **Используйте сильные пароли** для доступа к серверу
3. **Делайте регулярные бэкапы** - минимум еженедельно
4. **Мониторьте логи** - настройте алерты на подозрительную активность
5. **Документируйте изменения** - ведите changelog конфигурации
6. **Тестируйте восстановление** - проверяйте, что бэкапы работают
7. **Ограничьте SSH доступ** - только по ключам, смените порт
8. **Используйте 2FA** для критичных сервисов (опционально)

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел [Troubleshooting](#troubleshooting)
2. Изучите логи: `journalctl -u openvpn@server -n 100`
3. Проверьте конфигурацию: `openvpn --config /etc/openvpn/server.conf --test-crypto`

---

**Автор**: Traffic Shark VPN Infrastructure Team  
**Версия**: 1.0.0  
**Дата**: 2025-11-17  
**Лицензия**: MIT

